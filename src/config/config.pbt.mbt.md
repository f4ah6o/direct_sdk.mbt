<!-- aletheia:begin -->
# Property-Based Test Targets for config

> Auto-generated by Aletheia - MoonBit PBT Tool

## Package: config

```mbt
test "prop_apply_defaults_idempotent" {
  assert_true @qc.property(fn(cfg : Config) {
    let first = apply_defaults(cfg)
    let second = apply_defaults(first)
    second.endpoint == first.endpoint &&
    second.timeout == first.timeout &&
    second.name == first.name
  }) {
    @qc.gen_map4(
      @qc.gen_ascii_string(),
      @qc.gen_ascii_string(),
      @qc.gen_int_range(0, 400000),
      @qc.gen_ascii_string(),
      fn(token, endpoint, timeout, name) {
        {
          token: token,
          endpoint: endpoint,
          proxy_url: None,
          timeout: timeout,
          name: name,
          debug: false
        }
      }
    )
  }
}
```

```mbt
test "prop_validate_rejects_empty_token" {
  assert_true @qc.property(fn(cfg : Config) {
    match validate({ token: "", endpoint: cfg.endpoint, proxy_url: cfg.proxy_url, timeout: cfg.timeout, name: cfg.name, debug: cfg.debug }) {
      Err(msg) => msg.contains("Token")
      Ok(_) => false
    }
  }) {
    @qc.gen_map3(
      @qc.gen_ascii_string(),
      @qc.gen_int_range(0, 400000),
      @qc.gen_ascii_string(),
      fn(endpoint, timeout, name) {
        {
          token: "dummy",
          endpoint: endpoint,
          proxy_url: None,
          timeout: timeout,
          name: name,
          debug: false
        }
      }
    )
  }
}
```

```mbt
test "prop_validate_with_token_succeeds" {
  assert_true @qc.property(fn(token : String) {
    token != "" && validate(new_config(token)) is Ok
  }) {
    @qc.gen_filter(@qc.gen_ascii_string(), fn(t) { t != "" })
  }
}
```

```mbt
test "prop_validate_timeout_bounds" {
  assert_true @qc.property(fn(timeout : Int) {
    let cfg = { token: "test", endpoint: "wss://example.com", proxy_url: None, timeout: timeout, name: "test", debug: false }
    match validate(cfg) {
      Err(msg) => timeout < 100 || timeout > 300_000
      Ok(_) => timeout >= 100 && timeout <= 300_000
    }
  }) {
    @qc.gen_int_range(0, 400000)
  }
}
```

```mbt
test "prop_validate_endpoint_format" {
  assert_true @qc.property(fn(endpoint : String) {
    let cfg = { token: "test", endpoint: endpoint, proxy_url: None, timeout: 10000, name: "test", debug: false }
    match validate(cfg) {
      Err(msg) => !endpoint.has_prefix("ws://") && !endpoint.has_prefix("wss://")
      Ok(_) => endpoint.has_prefix("ws://") || endpoint.has_prefix("wss://")
    }
  }) {
    @qc.gen_one_of([
      @qc.gen_return("ws://localhost"),
      @qc.gen_return("wss://api.example.com"),
      @qc.gen_return("http://example.com"),
      @qc.gen_return("invalid"),
      @qc.gen_return("")
    ])
  }
}
```

## Notes

- Generated by Aletheia for config validation and apply_defaults properties
<!-- aletheia:end -->
